# Системний трей - Посібник користувача

## 🎯 Що це таке?

Програма CID Ретранслятор тепер може працювати в системному треї Windows! Це дозволяє:
- 🚀 Запускати програму згорнутою
- ➖ Згортати вікно в трей замість панелі задач
- ❌Ховати програму в трей при натисканні "Закрити" (не виходячи з програми)

## 📋 Швидкий старт

### Варіант 1: Через GUI (найпростіший)

1. Запустіть програму
2. Відкрийте вкладку **"Налаштування"**
3. Прокрутіть до секції **"Налаштування інтерфейсу"**
4. Позначте потрібні опції:
   - ✅ **Запускати згорнутим в трей**
   - ✅ **Згортати в трей при мінімізації**
   - ✅ **Згортати в трей при закритті**
5. Натисніть кнопку **"Зберегти"**
6. Перезапустіть програму

### Варіант 2: Через конфігураційний файл

Відредагуйте `config.yaml`:

```yaml
ui:
    startminimized: true   # Запуск згорнутим в трей
    minimizetotray: true   # Згортати в трей при мінімізації
    closetotray: true      # Згортати в трей при закритті
```

Збережіть файл і перезапустіть програму.

## 🖱️ Як користуватися треєм

### Показати/сховати вікно
- **Лівий клік** на іконці в треї - перемикає видимість вікна

### Контекстне меню
- **Правий клік** на іконці в треї відкриває меню:
  - **Показати/Сховати** - переключити вікно
  - **Вихід** - закрити програму повністю

### Поведінка кнопок вікна

**Без налаштувань трея (стандартно):**
- Кнопка мінімізації ➖ → вікно згортається на панель задач
- Кнопка закриття ❌ → програма закривається

**З увімкненими налаштуваннями трея:**
- Кнопка мінімізації ➖ → вікно ховається в трей (якщо `minimizetotray: true`)
- Кнопка закриття ❌ → вікно ховається в трей (якщо `closetotray: true`)
- Для повного виходу → правий клік на іконці трея → "Вихід"

## 💡 Корисні сценарії використання

### Сценарій 1: Фонова робота
```yaml
ui:
    startminimized: true
    closetotray: true
```
Програма запускається в треї і працює у фоні. Випадково не закриється при натисканні ❌.

### Сценарій 2: Швидкий доступ
```yaml
ui:
    minimizetotray: true
    closetotray: false
```
Можна швидко сховати вікно в трей, але кнопка ❌ закриває програму як звичайно.

### Сценарій 3: Звичайна робота (за замовчуванням)
```yaml
ui:
    startminimized: false
    minimizetotray: false
    closetotray: false
```
Програма працює як звичайна Windows програма без трея.

## 🎨 Зміна іконки

Іконка в системному треї - це файл `icon.ico` в директорії програми.

### Швидкий спосіб (без пересбірки)
1. Замініть файл `icon.ico` на свою іконку
2. Перезапустіть програму
3. Іконка завантажиться з файлу

### Професійний спосіб (вбудувати в .exe)
1. Замініть файл `icon.ico`
2. Виконайте:
   ```bash
   .\build.bat
   ```
3. Іконка буде вбудована в `.exe` файл

**Формат іконки:**
- Файл має бути `.ico` формату
- Рекомендовані розміри: 16x16, 32x32, 48x48, 256x256
- Windows автоматично вибере потрібний розмір

## ⚙️ Технічні деталі

**Завантаження іконки (автоматичний fallback):**
1. Спочатку програма намагається завантажити вбудовану іконку з `.exe`
2. Якщо не знайдена - завантажує з файлу `icon.ico`
3. Якщо файл відсутній - використовує стандартну Windows іконку

**Перевірка в логах:**
```
INFO: "Icon loaded from embedded resources"  ← ✅ Найкраще
INFO: "Icon loaded from file icon.ico"       ← ⚠️ Працює
INFO: "Using default Windows icon"           ← ⚠️ Стандартна
```

## ❓ Часті питання

**П: Чому іконка не з'являється в треї?**
В: Перевірте що файл `icon.ico` існує або іконка вбудована в `.exe`. Дивіться логи програми.

**П: Як повністю вийти з програми якщо увімкнено `closetotray`?**
В: Правий клік на іконці в треї → "Вихід"

**П: Програма запускається згорнутою, як показати вікно?**
В: Клікніть на іконку в системному треї (біля годинника)

**П: Можна змінити іконку без пересбірки?**
В: Так! Просто замініть файл `icon.ico` і перезапустіть програму.

**П: Де знаходиться іконка трея?**
В: Шукайте біля годинника в правому нижньому куті екрану (може бути прихована - клікніть стрілку вгору ˄)

## 📚 Додаткова документація

- [`ICON_README.md`](ICON_README.md) - Детальна інформація про роботу з іконками
- [`README_BUILD.md`](README_BUILD.md) - Інструкції по збірці програми
- [`walkthrough.md`](.gemini/antigravity/brain/.../walkthrough.md) - Технічна документація

## ✨ Підтримка

Якщо виникли проблеми:
1. Перевірте логи програми (`app.log`)
2. Переконайтеся що файл `icon.ico` існує
3. Спробуйте перезапустити програму
4. Перевірте що налаштування збережені в `config.yaml`

---

**Приємного користування! 🎉**
